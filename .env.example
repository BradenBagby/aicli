# Linear worker configuration
LINEAR_API_KEY=lin_api_xxxxx
LINEAR_TEAM_ID=your-team-uuid-here

# Optional (defaults shown)
# LINEAR_POLL_INTERVAL=30000
# LINEAR_MAX_ITERATIONS=10
# LINEAR_MODEL=sonnet

# Claude CLI auth is handled by the container's own login — no API key needed here
